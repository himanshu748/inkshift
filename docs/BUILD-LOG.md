# Build log

## September 20, 2026 — Direction and evidence
The user approved INKSHIFT for DEV's Sanity Challenge. Source review confirmed the Path Two rubric and that App SDK / Workflows are encouraged rather than mandatory. The first correction was separating physical spaces from sessions so moving a game does not replace its registrations. A missing region in a photograph is not evidence of deletion. Every change approval must recheck the current live revision.

The workspace started empty. No handwritten photographs or Sanity credentials were supplied. Implementation begins with explicit sample data and tests; real provider verification will be recorded separately.

## September 20, 2026 — Working prototype

Built the Next.js organizer, participant signup, photo review, manual correction, and proposal history. Stable space/session/registration IDs make relocation explicit. Sanity revision checks protect capacity and approval freshness; writes update the private event and linked public projections atomically.

Claimed Sanity project a5xdqsb7 and tested Content Lake transactions. A race for the last place had one winner; stale approval failed; five booking IDs survived relocation. Anonymous private-document reads were rejected. The local schema validates and extracts, but remote schema deployment was denied by token permissions.

Connected Qwen3-VL through the Hugging Face inference router. Corrected the provider's JSON-schema compatibility issue by expressing bounding boxes as fixed-length homogeneous arrays. Two real HTTP image calls produced a plan and an edit from rendered typed samples. The edit needed manual identity correction; the app blocked approval until resolved. Physical handwriting remains untested.

## September 20, 2026 — Requested Three.js landing

Applied the user-selected build-threejs-scroll-worlds workflow. Wrote the world bible and executable camera ledger, then built one persistent procedural miniature with four native-scroll chapters. The paper, tables and registration counters share identities. The same board and people move from B to C, and reverse scroll restores B. The landing buttons create real events.

Browser verification exercised desktop, tablet and phone CSS layouts, real signup/review/approval, participant updates and the direct App SDK inspector. Corrected navigation stacking, phone composition, height-only resize measurement, fallback highlight feedback, and first-viewport photograph access. Removed decorative heading eyebrows and the hard CTA shadow after the independent finish review. Increased two undersized metadata rules to 11px.

Prepared the source, verification record, architecture, demo script and unpublished DEV article. Automatic approval review blocked uploading the project to Vercel until the user explicitly approves the destination and data. No public deployment or challenge submission is claimed.

## September 21, 2026 — Public deployment

The user approved the Vercel upload, including private Sanity and Hugging Face environment variables, and completed CLI sign-in. Created the INKSHIFT project and deployed it at https://inkshift.vercel.app. Fixed the initial Other framework preset by committing the explicit Next.js deployment configuration. The exact production origin is allowed in Sanity CORS without credentials.

The hosted HTTP photo flow passed with two real vision calls against typed sample sheets. Its second reading attempted to remove the booked game; review blocked approval until an explicit operator correction restored the session. The same booking then appeared at Table C. Anonymous organizer and photo requests were denied. A separate browser flow exercised sample creation, invite signup, prepared review, approval, participant updates and the direct App SDK inspector.

The live inspector exposed an SDK server-rendering warning during production verification. Moved the subscription provider behind a browser-only import, redeployed, and checked the fixed inspector and existing participant booking. TypeScript, ESLint, all 19 domain tests and the Vercel production build passed. Refreshed the unpublished DEV draft with the verified demo URL. Source publication, a physical-paper video and the DEV submission remain pending.

## September 22, 2026 — Claude takeover

Recovered Claude's actual session, its working copy in `/Users/himanshujha/n/inkshift`, and the user's choices: a product-led landing without 3D, public source after checks, redeployment after verification, and Sanity Studio/schema login left as a handoff. Claude had authored the Workflows definition and adapter but had not wired the routes or interface. No new concept pivot was introduced.

Connected proposal creation, image readings, corrections, approval, discard and saved-review retrieval to the engine. Separated prepared-example attribution from the photo reader. Added recovery from a saved plan decision whose workflow follow-up fails. Deployed definition v1, passed 25 tests, and exercised the live workflow and actual typed-image path through the local production build.

Replaced the Three.js landing with a DOM walkthrough showing paper, participant join, review and relocation. Removed the renderer and its dependencies. Prepared a new DEV article from the verified build, including the incomplete handwriting test and the pending Studio/schema handoff.

The replacement passed desktop and mobile viewport review, followed by the independent reviewer’s documentation-fix verdict. The specialized Impeccable agent roles were unavailable, so generic agents used the prescribed fallback contracts. Vercel deployment `dpl_BY9ohBLjZmnWqDYY6Z88W47s4WVY` is Ready at the existing public URL. Hosted workflow and typed-photo HTTP checks passed, including stale review, preserved booking, discard and privacy checks. The fresh browser confirmed the product landing, saved review and connected App SDK inspector.
