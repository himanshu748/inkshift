# DEV draft status

`DEV-POST.md` is the current unpublished Path Two draft. `DEV-DRAFT.md` points to it. The physical-paper recording and DEV publication remain pending. Public source and deployment links are checked at release.

## What changed

Replaced the obsolete 3D demo description with the product walkthrough and actual Sanity Workflows integration. Added Claude's incomplete integration and Codex's continuation, the example/reader attribution correction, saved review recovery and the six workflow tests. Preserved the booked-session example, solo I/my voice, typed-input limitation, model identity failure and remote schema/Studio handoff.

## No-ai-slop evaluation

Read the full draft and checked it directly against the installed skill's eval.md after editing. Applicable checks pass: the supplied brief and verified results carry the article; no invented personal anecdote, user quote, accuracy number or successful handwriting test was added. Concrete failure and recovery details replace generic progress claims. The draft uses active verbs and varied sentence lengths, preserves required DEV template sections, and ends with the next physical-paper demonstration. A mechanical pass found no banned phrases or em dashes. Full publication copy and these notes remain separate. Detect-only checks do not apply.

The draft describes the same server credential with distinct declared caller contexts; it does not imply separate Sanity user accounts or database-enforced workflow guards. A public transcript is optional and has not been uploaded. The event permits one entry per path.
